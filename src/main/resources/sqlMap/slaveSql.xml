<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.sample.dao.SlaveDao">

	<select id="selectUser" parameterType="map" 
		resultType="io.sample.bean.model.UsersModel">
		SELECT
			user_id,
			user_name,
			user_pwd,
			user_status,
			insert_time,
			update_time
		FROM
			users
		WHERE
			user_name = #{userName}
	</select>

	<select id="selectUsersList" parameterType="map" 
		resultType="io.sample.bean.model.UsersModel">
		SELECT
			user_id,
			user_name,
			user_pwd,
			user_status,
			insert_time,
			update_time
		FROM
			users
	</select>

	<select id="selectAuth" parameterType="map" 
		resultType="io.sample.bean.model.auth.AuthModel">
		SELECT
			authority_id,
			authority,
			insert_time,
			update_time
		FROM
			authorities
		WHERE
			authority = #{authority}
	</select>

	<select id="selectAuthList" parameterType="map" 
		resultType="io.sample.bean.model.auth.AuthModel">
		SELECT
			authority_id,
			authority,
			insert_time,
			update_time
		FROM
			authorities
	</select>

</mapper>